const prompt = require("prompt-sync")();

/*
var mks=prompt("Obtained marks by the candidate: ");
let result;

switch(true){
    case mks > 100 || mks < 0:
        console.log("Wrong input");
        break;
    case mks <= 100 && mks > 90:
        result = "Grade-AA";
        break;
    case mks > 80:
        result = "Grade-A+";
        break;
    case mks > 70:
        result = "Grade-A";
        break;
    case mks > 60:
        result = "Grade-B+";
        break;
    case mks > 50:
        result = "Grade-B";
        break;
    case mks > 40:
        result = "Grade-C";
        break;
    case mks > 30:
        result = "Grade-D";
        break;
    case mks < 30:
        result = "Don't worry, you just failed in paper. Try next time. Best of luck for the real life exam";
        break;
}

console.log(result);
*/

switch(){
    
}
